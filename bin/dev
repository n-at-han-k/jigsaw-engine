#!/usr/bin/env ruby

system "docker compose up -d"
system("yarn build:css") unless File.exist?(File.join(__dir__, "..", "app", "assets", "stylesheets", "tailwind-build.css"))

exec "bundle", "exec", "falcon", "serve", "-b", "http://0.0.0.0:3000", *ARGV