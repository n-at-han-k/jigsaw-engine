#!/bin/bash -e

# If running the web server then create or migrate existing database
if [ "${@: -1:1}" == "./falcon.rb" ] || ( [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ] ); then
  ./bin/rails db:prepare
fi

exec "${@}"
