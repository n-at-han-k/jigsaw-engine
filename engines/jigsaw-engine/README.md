# Jigsaw

Rails engine with the `Jigsaw` namespace.

## Installation

Add to your Gemfile:

```ruby
gem "jigsaw_engine", path: "engines/jigsaw-engine"
```

Then:

```bash
bin/rails jigsaw_engine:install:migrations
bin/rails db:migrate
```
